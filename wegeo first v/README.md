# WeGeo — première version (archive)

Ce dossier contient la première version de WeGeo, telle qu'elle avait été
publiée ici : un build statique (`index.html` et `assets/`).

Elle est conservée uniquement pour mémoire et **n'est pas utilisée par la
version actuelle**, dont le code se trouve à la racine du dépôt.
